name: First Workflow
on: workflow_dispatch #event- manual trigger
jobs:
  first-job:
    runs-on: ubuntu-latest #runner
    steps:
      - name: Print greeting
        run: echo "Hello World"
      - name: Print goodbye
        run : echo "Done - bye!"
